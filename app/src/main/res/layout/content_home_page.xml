<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/rlContentHomePage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layout_behavior="@string/appbar_scrolling_view_behavior"
                tools:context="studio.crazybt.adventure.activities.HomePageActivity"
                tools:showIn="@layout/app_bar_home_page">

    <android.support.design.widget.TabLayout
        android:id="@+id/tlHomePage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tbHomePage"
        android:background="@color/white"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:tabSelectedTextColor="@color/primary"
        app:tabTextColor="@color/primary_background_content"
        android:elevation="2dp"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/vpHomePage"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/tlHomePage"
        android:background="@color/primary_background_content"/>


</RelativeLayout>